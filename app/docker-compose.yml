services:
  app:
    build: .
    expose:
      - "3000"
    env_file:
      - .env
    networks:
      - internal
    restart: unless-stopped

  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - app
  #   networks:
  #     - internal
  #   restart: unless-stopped

networks:
  internal:
    driver: bridge
